export type userType = "Admin" | "Editor" | "User";
export type test = userType | number | boolean;


export type TUserData = {
    name: string;
    age: number;
    city: string;
    username: string;
    password:string;
};